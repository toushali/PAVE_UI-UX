# Illustrations

Static line-art for exercises and modalities (no video — text + static
illustration + timer is the deliberate replacement, Section 1 / 6.9).

Inline the `<svg>` so `currentColor` picks up `--c-primary`. One icon
per modality; reused as the per-exercise art in the prototype.

| File | Modality |
| --- | --- |
| `mod-pilates.svg`       | Pilates |
| `mod-mobility.svg`      | Mobility |
| `mod-strengthening.svg` | Strengthening |
| `mod-breathing.svg`     | Breathing |

Also `physician.svg` — placeholder physician portrait (Onboarding, S9).
