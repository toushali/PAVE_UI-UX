{
  "name": "pave-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "PAVE — static, clickable UI/UX prototype. Patient PWA, Provider Portal and Platform Admin console. No backend, no framework, no build step — pure HTML/CSS/vanilla JS.",
  "scripts": {
    "dev": "npx --yes serve@latest -l 5500 .",
    "start": "npx --yes serve@latest -l 5500 ."
  }
}
